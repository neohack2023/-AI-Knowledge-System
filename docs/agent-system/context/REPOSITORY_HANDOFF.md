# Repository Semantic Handoff

State: `REPO_AUTONOMY_PHASE_2 / ORGANIZATIONAL_SKELETON_LIVE / CONTEXT_BUNDLE_CANDIDATE`

## Stable baseline

The repository organizational skeleton introduced by PR #70 is live on default branch. This handoff was materialized after merge commit `50f119cb11d0fcf05eb98f58d5e30fdc047a59e0`.

That SHA is a historical baseline, **not** a declaration of current `main`. Resolve current branch/PR/CI state live from GitHub.

## Current semantic phase

The repository is moving from:

`repo has instructions + organizational roles`

to:

`repo contains enough project context, governance projection, decisions, plans, and knowledge routing for normal agents to work without external memory retrieval`.

## Active concerns at materialization

- PR #69 remains an independent SENSITIVE exact-head CI correction and has a confirmed unresolved P2 on YAML/direct-child binding semantics. Do not treat it as merged or solved merely because its earlier repairs passed CI.
- PR #71 repairs the post-merge PR #70 organizational integrity findings: orthogonal evidence/promotion state and immutable lesson provenance.
- Repository self-sufficiency Phase 1 vendors a public-safe local context/governance bundle without cutting over global AIOS authority.

## Next repository actions

1. Land organizational-integrity repair with clean current evidence.
2. Land the local context/governance bundle.
3. Route tool-specific adapters to the local bundle.
4. Convert stable lifecycle commands into repository-native task skills where supported.
5. Add drift checks for handoff, governance lock, decision references, feature dossiers, and anti-pattern evidence edges.

## Resume rule

A new agent resumes from this file plus live GitHub state. It should not fetch Notion merely to reconstruct this phase.

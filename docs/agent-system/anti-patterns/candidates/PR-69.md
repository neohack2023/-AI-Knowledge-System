# PR #69 Anti-Pattern Seed

Source PR: `#69` — exact-head CI binding follow-up.  
Current source status at this seed update: open/unmerged.  
These records are local negative knowledge and do not self-promote.

## `AP-R69-001` — Regex proves nearby text instead of the actual binding
- **Source evidence:** head `8d0113828c1b1a512b158a65319cfd1692d46411`; review `PRR_kwDOTeqfeM8AAAABMPGj3A`; thread `PRRT_kwDOTeqfeM6fYzrZ`; comment `3936490507`.
- **Observed behavior:** a regression could find the desired head-SHA expression near an `actions/checkout` occurrence without proving that expression was the checkout step's `with.ref`.
- **Repair:** bind the assertion structurally to the checkout step and its input mapping.
- **Guard:** changing only checkout `with.ref` must fail even if the desired SHA expression remains nearby.
- **Evidence state:** `VALIDATED_LOCAL` after repair on `92df4e65a5a2e984267b01f16f5690ef9a1143b7` and CI run `33902296786`.
- **Promotion state:** `NONE`.

## `AP-R69-002` — Regression checks the main clause but drops its exception
- **Source evidence:** head `8d0113828c1b1a512b158a65319cfd1692d46411`; review `PRR_kwDOTeqfeM8AAAABMPGj3A`; thread `PRRT_kwDOTeqfeM6fYzrg`; comment `3936490517`.
- **Observed behavior:** an instruction regression asserted stale-by-default behavior but did not protect the `unless transferability is mechanically proven` qualifier.
- **Repair:** assert the complete normative rule or a structured representation of both rule and exception.
- **Guard:** deleting either clause must fail.
- **Evidence state:** `VALIDATED_LOCAL` after repair on `92df4e65a5a2e984267b01f16f5690ef9a1143b7` and CI run `33902296786`.
- **Promotion state:** `NONE`.

## `AP-R69-003` — Nested YAML text impersonates a direct checkout input
- **Source evidence:** head `92df4e65a5a2e984267b01f16f5690ef9a1143b7`; review `PRR_kwDOTeqfeM8AAAABMPNggQ`; thread `PRRT_kwDOTeqfeM6fZB3U`; comment `3936579181`.
- **Observed behavior:** the strengthened regex still accepts a `ref:` line nested inside another `with` value such as a block scalar, rather than proving `ref` is a direct child of the checkout step's `with` mapping.
- **Why reasonable:** indentation-aware text matching appears structural without actually parsing YAML semantics.
- **Actual failure:** the test can pass while `actions/checkout` falls back to the merge ref.
- **Repair candidate:** parse YAML or require direct-child mapping semantics that cannot be satisfied by nested scalar text.
- **Guard candidate:** place the expected `ref:` text only inside another `with` value and require the regression to fail.
- **Evidence state:** `CONFIRMED`.
- **Promotion state:** `NONE`.

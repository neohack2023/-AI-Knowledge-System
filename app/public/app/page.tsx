import PublicWorkbench from "./public-workbench";

export const metadata = {
  title: "AIOS Public Workbench",
  description: "Explore public AIOS knowledge, governance principles, and contributor entry points.",
};

export default function PublicAppPage() {
  return <PublicWorkbench />;
}
